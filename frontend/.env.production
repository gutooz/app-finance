# URL do backend em PRODUCAO usada APENAS pelo build mobile (iOS/Android).
# No deploy web (Vercel) isto e ignorado: la o frontend usa a mesma origem.
# O app nativo NAO funciona com localhost aqui — precisa ser uma URL publica https.
VITE_API_URL=https://app-finance-guto1020.vercel.app
